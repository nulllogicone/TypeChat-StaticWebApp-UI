{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build && copyfiles -u 1 src/**/*Schema.ts src/**/*.txt dist/src",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "poststart": "copyfiles -u 1 src/**/*Schema.ts src/**/*.txt dist"
  },
  "engines": {
    "node": "^18.0.0"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@azure/static-web-apps-cli": "^1.1.4",
    "typechat": "^0.0.10"
  },
  "devDependencies": {
    "@types/node": "^18.x",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  },
  "main": "dist/src/functions/*.js"
}
