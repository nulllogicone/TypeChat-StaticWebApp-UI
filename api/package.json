{
  "name": "statictypechatweb",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "postbuild": "copyfiles -u 1 src/**/*Schema.ts src/**/*.txt dist",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build && copyfiles src/**/*Schema.ts src/**/*.txt dist",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "copyfiles": "^2.4.1",
    "dotenv": "^16.3.1",
    "typechat": "^0.0.10"
  },
  "devDependencies": {
    "@types/node": "^18.x",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  },
  "main": "dist/src/functions/*.js"
}
